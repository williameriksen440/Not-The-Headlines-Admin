import { GetTableDataSourcePipe } from './get-table-data-source.pipe';

describe('GetTableDataSourcePipe', () => {
  it('create an instance', () => {
    const pipe = new GetTableDataSourcePipe();
    expect(pipe).toBeTruthy();
  });
});
